import 'package:flutter/material.dart';

void main() => runApp(
      const MaterialApp(
        home: Center(
          child: Text(
            'hello World',
          ), // Flutter doc recommend using comma, after ')'
        ),
      ),
    );
